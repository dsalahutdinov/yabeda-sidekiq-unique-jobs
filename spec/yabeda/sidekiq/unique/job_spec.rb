RSpec.describe Yabeda::Sidekiq::Unique::Job do
  it "has a version number" do
    expect(Yabeda::Sidekiq::Unique::Job::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
